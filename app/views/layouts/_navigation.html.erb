<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <%= link_to "Base de Datos Teruel", root_path, class: "navbar-brand" %>
    
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>
    
    <div class="collapse navbar-collapse" id="navbarNav">
      <% if logged_in? %>
        <!-- Menú izquierdo -->
        <ul class="navbar-nav me-auto">
          <!-- Gestión -->
          <% if current_user && current_user.cargo == "Administrativo" %>
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                <i class="bi bi-gear"></i> Gestión
              </a>
              <ul class="dropdown-menu">
                <li>
                  <%= link_to users_path, class: "dropdown-item" do %>
                    <i class="bi bi-person-lines-fill"></i> Usuarios
                  <% end %>
                </li>
                <li>
                  <%= link_to sectors_path, class: "dropdown-item" do %>
                    <i class="bi bi-geo-alt-fill"></i> Sectores
                  <% end %>
                </li>
                  <li>
    <%= link_to varieties_path, class: "dropdown-item", title: "Variedades" do %>
      <i class="fa-solid fa-seedling"></i> Variedades
    <% end %>
  </li>
              </ul>
            </li>
          <% end %>
  
          <!-- Operaciones -->
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
              <i class="bi bi-clipboard-data"></i> Operaciones
            </a>
            <ul class="dropdown-menu">
              <li>
                <%= link_to irrigations_path, class: "dropdown-item" do %>
                  <i class="bi bi-droplet"></i> Riegos
                <% end %>
              </li>
              <li>
                <%= link_to receptions_path, class: "dropdown-item" do %>
                  <i class="bi bi-box-seam"></i> Recepciones
                <% end %>
              </li>
              <li>
                <%= link_to harvests_path, class: "dropdown-item" do %>
                  <i class="bi bi-flower3"></i> Cosecha
                <% end %>
              </li>
            </ul>
          </li>
  
          <!-- Inventario -->
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
              <i class="bi bi-archive"></i> Inventario
            </a>
            <ul class="dropdown-menu">
              <li>
                <%= link_to inventories_path, class: "dropdown-item" do %>
                  <i class="bi bi-card-list"></i> Itemizado
                <% end %>
              </li>
              <li>
                <%= link_to new_inventorie_history_path, class: "dropdown-item" do %>
                  <i class="bi bi-pencil-square"></i> Modificar Inventario
                <% end %>
              </li>
              <li>
                <%= link_to inventorie_histories_path, class: "dropdown-item" do %>
                  <i class="bi bi-clock-history"></i> Historial
                <% end %>
              </li>
            </ul>
          </li>
  
          <!-- Informes -->
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
              <i class="bi bi-file-earmark-text"></i> Informes
            </a>
            <ul class="dropdown-menu">
              <li>
                <%= link_to informe_receptions_path, class: "dropdown-item" do %>
                  <i class="bi bi-box-seam"></i> Recepciones
                <% end %>
              </li>
            </ul>
          </li>
        </ul>
  
        <!-- Menú derecho: Usuario y Cerrar Sesión -->
        <ul class="navbar-nav">
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
              <i class="bi bi-person-circle"></i>
              <%= current_user.nombre %> <%= current_user.apellido %>
            </a>
            <ul class="dropdown-menu dropdown-menu-end">
              <li>
                <div class="dropdown-item text-muted">
                  <i class="bi bi-briefcase"></i> <%= current_user.cargo %>
                </div>
              </li>
              <li>
                <div class="dropdown-item text-muted">
                  <i class="bi bi-person-badge"></i> <%= current_user.contrato.capitalize %>
                </div>
              </li>
              <li><hr class="dropdown-divider"></li>
              <li>
                <%= button_to logout_path, 
                    method: :delete, 
                    class: "dropdown-item text-danger",
                    form: { data: { turbo: "false", turbo_confirm: "¿Estás seguro que deseas cerrar sesión?" } } do %>
                  <i class="bi bi-box-arrow-right"></i> Cerrar Sesión
                <% end %>
              </li>
            </ul>
          </li>
        </ul>
      <% end %>
    </div>
  </div>
</nav> 